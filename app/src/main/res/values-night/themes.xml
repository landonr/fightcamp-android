<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.FirstApp" parent="Theme.AppCompat.DayNight.NoActionBar"/>
    <!-- Base application theme. -->
    <style name="PrimaryText" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/brand_gray0</item>
        <item name="android:textSize">@dimen/large_title_size</item>
        <item name="fontFamily">@font/graphik</item>
    </style>
    <style name="SecondaryText" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/brand_gray0</item>
        <item name="android:textSize">@dimen/small_title_size</item>
        <item name="fontFamily">@font/graphik_medium</item>
    </style>
</resources>